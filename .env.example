# Firebase Configuration
# Copy this file to .env.local and fill in your Firebase credentials
#
# Get these values from: https://console.firebase.google.com
# 1. Create a new project (or select existing)
# 2. Click "Add app" > Web (</>)
# 3. Copy the config values below
# 4. Go to Firestore Database > Create database
# 5. Set security rules (see CLAUDE.md for recommended rules)

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Base path for deployment (optional)
# Default: / (for Firebase Hosting)
# Set to /rsvp/ for GitHub Pages (repo name)
# VITE_BASE_PATH=/
