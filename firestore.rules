rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rsvps/{rsvpId} {
      // Only allow create if document ID matches the email field
      allow create: if request.resource.data.email == rsvpId;
      allow read, update, delete: if false;
    }
  }
}
